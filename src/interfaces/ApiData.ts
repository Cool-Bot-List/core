export default interface ApiData {
  /**
   * The message that could come back from the API.
   */
  message?: string;
  /**
   * The error that could come back from that API.
   */
  error?: string;
}
